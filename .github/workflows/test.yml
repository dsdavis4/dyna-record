name: Run unit tests

on:
  pull_request:
    types:
      - opened
      - reopened
      - edited
      - synchronize

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: ["22", "24"]
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Set up Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run build
        run: npm run build

      - name: Run unit tests with coverage
        run: npm test -- --coverage

  # Aggregator job: publishes a single stable status check name (`run-tests`)
  # regardless of the matrix shape. Branch protection on main can target this
  # one name so it doesn't have to be updated every time the matrix changes
  # (e.g., adding Node 26, dropping Node 22).
  run-tests:
    name: run-tests
    needs: test
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Verify matrix succeeded
        run: |
          if [ "${{ needs.test.result }}" != "success" ]; then
            echo "Matrix jobs did not all succeed (result: ${{ needs.test.result }})"
            exit 1
          fi
          echo "All matrix entries passed."
